# Local SQLite file. For production point this at Postgres and change the
# provider in prisma/schema.prisma.
DATABASE_URL="file:./prisma/dev.db"

# Shared password for the editor console at /admin.
# Placeholder auth — replace with real accounts before this is public.
ADMIN_PASSWORD="change-me"
